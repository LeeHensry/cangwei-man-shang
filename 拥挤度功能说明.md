# 仓位满上 - 拥挤度雷达功能说明

## 更新日期
2026-07-28

## 新增文件

| 文件 | 说明 |
|------|------|
| `src/factors/crowding.js` | 拥挤度因子计算模块（核心） |
| `web/src/pages/Crowding.jsx` | 拥挤度雷达前端页面 |

## 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/data/db.js` | 新增 `crowding_score` 表、`sector_crowding` 表；`stock_score` 表新增 `crowding_score`、`crowding_level` 字段 |
| `src/strategies/value_score.js` | 评分引擎集成拥挤度；新增 `momentum_buy` 信号；动量搭车加分+拥挤惩罚分 |
| `server.js` | 新增 `/api/crowding/overview`、`/api/crowding/stock/:code`、`/api/crowding/calc` 三个API；新增 `/crowding-radar` 服务端渲染页面；修改静态文件缓存策略（index.html动态注入版本号参数） |
| `web/src/App.jsx` | 新增「📡 拥挤度雷达」菜单项和路由 |
| `web/src/pages/Dashboard.jsx` | 首页顶部增加拥挤度预警/搭车候选横条；信号分布增加动量搭车统计 |
| `web/src/pages/Signals.jsx` | 信号筛选增加「🟣动量搭车」；表格新增拥挤度列 |
| `web/src/api/index.js` | 新增3个拥挤度API调用函数 |
| `web/src/main.jsx` | 新增signal-momentum_buy样式 |
| `web/vite.config.js` | 明确配置hash文件名输出 |

## 核心功能逻辑

### 策略原理
利用量化资金的「自我强化→一致性行动→拐点踩踏」行为模式：
1. **搭车**：量化刚开始涌入（拥挤度30-55°），小仓位顺势介入
2. **持有**：趋势延续（55-75°），不追加
3. **减仓**：拥挤到极端（75-90°），主动减仓50%
4. **清仓**：踩踏风险极高（90°+），立即清仓

### 6个拥挤度因子（个股级）

| 因子 | 权重 | 计算方式 |
|------|------|---------|
| 动量加速度 | 25% | 近5日涨幅/近20日涨幅的比值，>1.8加速赶顶，<0.5上涨减速 |
| 换手率分位 | 20% | 当前换手率在近3年中的百分位，>90%为极端活跃 |
| 量价背离 | 15% | 价创新高但量能萎缩(顶背离)，放量暴跌(出货)，天量新高(冲顶) |
| 连涨天数 | 10% | 连续上涨天数，≥5天为持续逼空 |
| 5日绝对涨幅 | 15% | 短期暴涨是最直接拥挤信号(≥25%满分) |
| 20日绝对涨幅 | 15% | 中期涨幅(≥50%满分) |

### 板块级拥挤度
- 板块内个股拥挤度均值
- **同涨同跌度**：板块内80%以上个股同涨/同跌=一致性行动+15分
- **龙头滞涨信号**：板块龙头20日涨幅>20%但5日动量减速=龙头先死+20分

### 信号类型

| 信号 | 条件 | 操作建议 |
|------|------|---------|
| 🟢 buy | 价值/估值/技术达标 | 建仓10-15% |
| 🟣 momentum_buy | 拥挤度30-55°+技术面≥50+非老登股+总分≥45 | 小仓5-8%，-7%严格止损，拥挤度到80止盈 |
| 🟡 watch | 基本面尚可但未到买点 | 观察 |
| ⏸ hold | 评分50-65 | 持有不动 |
| ↓ sell | 拥挤度≥75°或估值极高或基本面恶化 | 减仓/清仓 |

### 硬风控规则
- 拥挤度≥90° → 强制sell信号，不管基本面
- 拥挤度≥75°且拐头 → 强制减仓信号
- 银行/保险/地产/基建/钢铁/煤炭/石油（老登股）不参与动量搭车
- 放量下跌(high_volume_decline) → 立即exit
- 动量搭车票：目标+20%，止损-7%，持仓≤5-8%

## API接口

### GET /api/crowding/overview
返回全市场拥挤度数据：
- `market_avg_crowding`: 全市场平均拥挤度(0-100)
- `market_crowding_level`: cold/warm/hot/crowded/extreme
- `sector_levels`: 各等级板块数量
- `all_sectors`: 全部板块拥挤度列表
- `exit_signals`: 清仓预警个股
- `trim_signals`: 减仓预警个股
- `momentum_candidates`: 动量搭车候选
- `cold_opportunities`: 冷清逆向机会

### GET /api/crowding/stock/:code
返回单只股票拥挤度详情+近20日历史

### POST /api/crowding/calc
触发拥挤度重新计算（异步）

### GET /crowding-radar
服务端渲染的纯HTML拥挤度页面（不经过React，无缓存问题）

## 使用方式

1. 解压zip到原有stock-advisor目录覆盖
2. 启动服务：`cd stock-advisor && node server.js`
3. 首次访问会自动创建新表，触发一次拥挤度计算（POST /api/crowding/calc）
4. 浏览器访问 http://localhost:3001/
5. 左侧菜单点「📡 拥挤度雷达」查看完整页面
6. 或直接访问 http://localhost:3001/crowding-radar 查看独立页面

## 缓存问题解决方案

服务器已配置：
- **index.html**: `no-cache, no-store` 不缓存，每次请求服务器
- **JS/CSS URL**: 每次服务重启自动加 `?v=时间戳` 参数，浏览器强制下载新版本
- 以后前端更新只需：`cd web && npm run build` + 重启node服务，用户端自动加载新版本

## 拥挤度等级说明

| 分数 | 等级 | 颜色 | 操作 |
|------|------|------|------|
| 0-30 | 冷清🧊 | 绿色 | 优质股逆向布局 |
| 30-55 | 搭车🟣 | 紫色 | 小仓位顺势介入(-7%止损) |
| 55-75 | 火热🔥 | 橙色 | 持有不追加 |
| 75-90 | 预警⚠️ | 红色 | 减仓50% |
| 90-100 | 极端🚨 | 深红 | 清仓离场 |
