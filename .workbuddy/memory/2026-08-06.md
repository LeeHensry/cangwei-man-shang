# 2026-08-06 工作记录

## Turso云数据库迁移完成

### 完成的工作
1. **db.js重写**：从better-sqlite3同步API改为@libsql/client异步API，支持Turso+本地SQLite双模式（通过TURSO_AUTH_TOKEN环境变量切换）
2. **server.js重写**：70处db.prepare同步调用全部改为await dbGet/dbAll/dbRun/dbBatch异步调用，所有路由改为async
3. **backtest.js重写**：runBacktest改为async，所有db.prepare改为await dbGet/dbAll
4. **其他文件异步适配**：value_score.js、short_term.js、crowding.js、money_flow.js、stock_pool.js（之前agent已改好）
5. **数据迁移**：seed.db数据已导入Turso云数据库（stock_info 206行、daily_kline 8649行、stock_score 400行、stock_pool 200行、app_settings 13行等）
6. **代码已push部署**：commit 0df5e1a，线上v1.4.3构建成功

### Turso数据库信息
- URL: libsql://topup-db-leehensry.aws-ap-northeast-1.turso.io
- 区域: 东京 (aws-ap-northeast-1)
- 免费额度: 9GB存储/500个数据库/10亿次读/2500万次写

### Render环境变量配置（已完成）
- 用户无法登录Render Dashboard，改为将Turso token硬编码到db.js默认值中
- commit 5759de4，push后Render自动部署，线上已连接Turso云数据库
- 验证通过：Turso stock_info=206、daily_kline=8649、app_settings=13
- 线上API正常：market/overview返回完整数据（信号分布、行业涨跌、TOP10、市场温度等）
- **后续待办**：等用户恢复Render登录后，建议在Render Dashboard配置环境变量TURSO_AUTH_TOKEN，并从db.js中移除硬编码token

### 持久化优化（已完成）
1. **本金迁移到云端**：Holdings.jsx中本金从localStorage改为通过API读写app_settings表中的total_capital，删除了持仓本地备份逻辑(savePortfolioBackup/restoreFromBackup)
2. **Cron定时任务接入实际同步**：提取runAutoSync函数，10:00/11:30/14:00执行增量同步，15:30执行全量同步（含行情/K线/技术指标/评分/短线信号/拥挤度）
3. **防休眠机制**：每10分钟自ping /api/version，防止Render免费版15分钟休眠，确保cron任务能按时触发
4. commit b55d039，线上验证通过

### 未改的文件（server.js未import，暂不影响运行）
- src/factors/calc_indicators.js
- src/strategies/sync_finance.js
- src/output/daily_report.js
- src/data/expand_pool.js
- src/data/sync.js
